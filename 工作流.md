这是一个本地 FLAC 标签写入任务。

处理当前文件夹内所有古典音乐 FLAC 文件。
严格限制执行范围。

禁止执行以下任何操作：
- 禁止访问互联网
- 禁止 WebSearch
- 禁止调用 MusicBrainz、Discogs、任何在线数据库
- 禁止查询外部专辑信息
- 禁止安装软件
- 禁止检查系统环境
- 禁止搜索 DLL
- 禁止修改 PATH
- 禁止检查 Python/node 环境
- 禁止复制任何文件
- 禁止修改任何非目标 FLAC 文件
- 禁止执行与任务无关的 Bash/PowerShell 命令

严格按照一下指示进行操作，不要进行任何的自发的处理。
允许执行的唯一操作：

目标：
读取对应的 backXXX.jpg 背封信息，生成标准 Vorbis Comment 标签，并使用当前目录下的 metaflac.exe 写入对应 FLAC 文件。

执行流程：
1. 扫描当前目录：
- 找出所有 .flac 文件
- 找出所有 backXXX.jpg 文件
- 不需要展开扫描更深层级
- 仅扫描访问flac和jpg文件

2. 建立对应关系：
例如：
013 Mozart Flute Concertos Nos. 1 & 2 · Concerto for Flute & Harp.flac
对应：
back013.jpg
根据文件名前三位数字匹配。
注意：backXXX.jpg会有多余的，无对应的flac文件，此时请忽略这些backXXX.jpg

3. 对每张 backXXX.jpg：
使用 OCR + 图片理解读取背封内容。

提取：
- 专辑英文标题
- 每首曲目：
  - 作品名 WORK
  - 乐章 MOVEMENT
  - 作曲家 COMPOSER
  - 指挥 CONDUCTOR
  - 乐团（合唱团） ORCHESTRA
  - 独奏家 SOLOIST
  - 录音年份 YEAR

4. 生成标签格式：
ALBUM=英文专辑名
COMMENT=专辑介绍
CUE_TRACK01_WORK=
CUE_TRACK01_MOVEMENT=
CUE_TRACK01_COMPOSER=
CUE_TRACK01_CONDUCTOR=
CUE_TRACK01_ORCHESTRA=
CUE_TRACK01_SOLOIST=
CUE_TRACK01_YEAR=

后续曲目依次增加 TRACK02、TRACK03……

5. 写入 FLAC：
使用当前目录下的flac-1.5.0-win文件夹内的metaflac.exe
.\metaflac.exe
执行：
--set-tag="字段=内容"
不要重新编码音频。
不要修改文件名。
不要删除已有tag。

6. 输出日志：
格式：
[OK] 013 Mozart Flute Concertos...
写入标签数量：
TRACK数量：
失败：
[ERROR] 文件名
原因：
